server:
  port: 8086
spring:
  application:
    name: userwallet
  datasource:
    password: password
    url: jdbc:postgresql://localhost:5432/userwallet
    username: amigoscode
    driver-class-name: org.postgresql.Driver
  zipkin:
    base-url: http://localhost:9411
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    fetch-registry: true
    register-with-eureka: true
    enabled: true
snowflake:
  workerId: 3



